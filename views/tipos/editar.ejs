<%- include('../partials/header') %>

<div class="container mx-auto py-8">
  <h2 class="text-xl font-bold mb-4">Editar Tipo de Usuario</h2>

  <% if (error) { %>
    <div class="text-red-500 mb-4"><%= error %></div>
  <% } %>

  <form action="/tipos/editar/<%= tipo.ID_TIPO %>" method="POST" class="max-w-md">
    <input type="text" name="NOMBRE" value="<%= tipo.NOMBRE %>" required
      class="w-full border px-3 py-2 mb-4 rounded">
    <div class="flex justify-end">
      <a href="/tipos" class="bg-gray-500 text-white px-4 py-2 rounded mr-2">Cancelar</a>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Actualizar</button>
    </div>
  </form>
</div>
